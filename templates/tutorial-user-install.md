Type this into the message box and press Enter (one line at a time):

```
/plugin marketplace add {{company.github_repo}}
```

```
/plugin install {{catalog.example_plugin}}@{{company.marketplace_name}}
```

The first line connects you to the company catalog (you only ever do it once). The second installs a plugin — swap `{{catalog.example_plugin}}` for any name from your catalog.
